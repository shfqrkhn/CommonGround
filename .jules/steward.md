## 2024-03-27 - [Palette Mode] - [Prevented double-tap zoom on all links]
**Protocol:** Ensure `a` tags also have `touch-action: manipulation` by default in global CSS to prevent fast interaction zoom.
